import {StyleSheet} from 'react-native';

const employeeScreenStyle = StyleSheet.create({
  topContainer: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  firstContainer: {
    width: '70%',
  },
  secondContainer: {
    backgroundColor: '#999',
    width: '30%',
  },
});

export default employeeScreenStyle;
