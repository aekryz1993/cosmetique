import {StyleSheet} from 'react-native';

const tabCategory = StyleSheet.create({
  container: {
    padding: 10,
    paddingBottom: 0,
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});

export default tabCategory;
